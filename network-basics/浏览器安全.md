* 浏览器安全可以分为三大块: Web页面安全  浏览器网络安全  浏览器系统按钮
# 同源策略
* 如果两个URL的协议 域名 端口 都相同, 我们就称为这两个URL同源。
* 浏览器默认两个相同的源之间是可以相互访问资源和操作DOM的。
* 两个不同的源之间若想要相互访问资源或者操作DOM，同源策略会制约。

1. DOM
2. 数据层面
3. 网络层面


## 浏览器出让了同源策略的哪些安全性
1. 页面中可以嵌入第三方资源   同源策略要让一个页面的所有资源都来自同一个源,也就是要将该页面的所有资源html js css img等资源都部署在同一台服务器上。
2. 



## 内容安全策略( CSP )


## 跨站脚本攻击(XSS) 为什么Cookie中需要HttpOnly属性

* 同源策略可以隔离各个站点之间的DOM交互 页面数据和网络通信。
* 默认页面中可以引用任意第三方资源 然后引入 CSP
* 默认XMLHttpRequest Fectch不能跨站请求资源   CORS策略


* 注入恶意JavaScript脚本
1. 窃取Cookie信息  
    - document.cookie获取Cookie信息,然后通过XMLHttpRequest或者Fetch加上CORS功能将数据发送给恶意服务器；
    - 恶意服务器拿到用户的Cookie信息之后,就可以在其他电脑上模拟用户的登录,然后进行转账等操作。
2. 监听用户行为
    - 使用 addEventListener接口监听键盘事件,比如可以获取用户输入的信用卡等信息,将其发送给恶意服务器。黑客掌握了这些信息之后,又可以做很多违法的事情。
3. 修改DOM 伪造假的登录窗口 用来欺骗用户输入用户名和登录等信息
4. 还可以在页面生成浮窗广告 这些广告会严重地影响用户体验。


* XSS的类型
1. 存储型XSS攻击
2. 反射型XSS攻击 Web服务器不会存储发射型XSS攻击的恶意脚本,这是和存储型XSS攻击不同的地方。
3. 基于DOM的XSS攻击   网络劫持在页面传输过程中修改html页面的内容


* 如何阻止XSS攻击

* 无论何种类型的XSS攻击,他们都有一个共同点,那就首先往浏览器中注入恶意脚本,然后再通过恶意脚本将用户信息发送至黑客部署的恶意服务器上。


* 阻止XSS攻击,我们可以通过阻止恶意JavaScript脚本的注入和恶意消息的发送来实现
1. 服务器对输入的脚本进行过滤或转码
2. 充分利用CSP
    - 限制加载其他域下的资源文件,这样即使黑客插入一个javascript文件,这个js文件也无法加载
    - 禁止向第三方域提交数据
    - 禁止直行行内脚本和未授权的脚本
    - 还提供上报机制,这样可以帮助我们尽快发现有哪些XSS攻击,以便尽快修复问题。
3. 使用HttpOnly 属性
* 通常服务器可以将某些Cookie设置为HttpOnly标志,HttpOnly是服务器通过HTTP响应头来设置的。


* CSR攻击:陌生链接不要随便点
* CSRF:跨站请求伪造  是指黑客引诱用户打开黑客的网站,在黑客的网站中,利用用户的登录状态发起的跨站请求。
* 简单来讲,CSRF攻击就是黑客利用了用户登录状态,并通过第三方的站点来做一些坏事。

* 有以下几个攻击手段
1. 自动发起Get请求 
2. 自动发起POST请求
3. 引诱用户点击链接



* 发起CSRF攻击的三个必要条件
1. 目标站点一定有CSRF漏洞 
2. 用户要登录过目标站点,并且在浏览器上保持有该站点登录状态
3. 需要用户打开一个第三方站点,可以是黑客的站点，也可以是一些论坛


* 避免CSRF攻击几种途径
1. 充分利用好Cookie SameSite属性
* Cookie真是浏览器和服务器之间维护状态的一个关键数据。


* SameSite
    - Strict 完全禁止第三方Cookie。 
    - Lax 相对宽松  第三方链接打开 第三方提交Get方式的表单 都会提交Cookie。  Post img iframe 不会携带Cookie
    - None   任何情况下都会发送Cookie数据


 2. 验证请求的来源站点
    - Referer  记录HTTP请求的来源地址 
    - Origin  只包含域名信息 并没有包含具体的URL路径 
 3. CSRF Token     在浏览器向服务器请求时 服务器生成一个 CSRF Token。 当浏览器再次发起请求时, 带上 CSRF Token