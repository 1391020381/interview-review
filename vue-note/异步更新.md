* watcher执行 update()的时候
* queueWatcher(this) 也就是把 watcher保存到一个数组里面
* 每次执行 queueWatcher 都会通过一个 pending 变量 来控制执行  has  pending
* nextTick(flushSchedulerQueue)  把 flushSchedulerQueue保存数组中  pending 控制执行
* timerFunc() Promsie.resolve()  MutationObserver settimeout
